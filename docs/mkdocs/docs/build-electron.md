# Build Electron

## Setup

```sh
npm install
```

## Windows

Build executable files of this program for Windows:

```sh
cd scripts
./win_build.sh
```

The files can be found in the directory `dist`.
